name: common
description: A common library for database initialization and more.
version: 0.0.1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Env
  flutter_dotenv: ^5.1.0

  # mBaaS
  supabase_flutter: ^1.10.19
